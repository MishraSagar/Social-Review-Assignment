import React from 'react';
import Map from './Map';

class MapContainer extends React.Component {
    constructor(props) {
        super(props);
    }
    render() {
        return (
            <Map />
        );
    }
}

export default MapContainer;